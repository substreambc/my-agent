{
  "protocolVersion": "1.0",
  "name": "Sentinel",
  "description": "AI Intelligence Layer for the Helium Network. Queries a Solana-indexed Helium telemetry datalake.",
  "version": "1.0.0",
  "url": "https://a2a.sntl.site/api/v2",
  "preferredTransport": "JSONRPC",
  "provider": {
    "organization": "Sentinel",
    "url": "https://sntl.site"
  },
  "capabilities": {
    "streaming": false,
    "pushNotifications": false,
    "stateTransitionHistory": false
  },
  "defaultInputModes": ["text/plain"],
  "defaultOutputModes": ["application/json"],
  "securitySchemes": {
    "x402": {
      "type": "http",
      "scheme": "x402",
      "description": "Per-query settlement. Server returns 402 with accepts:[PaymentRequirements]; client resubmits with X-PAYMENT. Solana mainnet USDC, gasless via PayAI facilitator. 0.01 USDC per query."
    }
  },
  "security": [{ "x402": [] }],
  "skills": [
    {
      "id": "helium-telemetry-query",
      "name": "Helium Telemetry Query",
      "description": "Natural-language query against indexed Helium Network on-chain events on Solana.",
      "tags": ["helium", "solana", "telemetry", "datalake"],
      "inputModes": ["text/plain"],
      "outputModes": ["application/json"]
    }
  ]
}
